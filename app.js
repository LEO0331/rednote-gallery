const badges = [
  {
    image: "badges/IMG_9364.JPG",
    date: "2026-04-24",
    tag: "Milestone",
    title: {
      en: "RedNote Snapshot 9364",
      "zh-TW": "小紅書里程碑快照 9364",
      "zh-CN": "小红书里程碑快照 9364"
    },
    description: {
      en: "Captured a progress checkpoint for account growth.",
      "zh-TW": "記錄帳號成長進度的關鍵節點。",
      "zh-CN": "记录账号成长进度的关键节点。"
    }
  },
  {
    image: "badges/IMG_9365.JPG",
    date: "2026-04-24",
    tag: "Badge",
    title: {
      en: "RedNote Snapshot 9365",
      "zh-TW": "小紅書勳章快照 9365",
      "zh-CN": "小红书勋章快照 9365"
    },
    description: {
      en: "Saved an achievement screen for portfolio records.",
      "zh-TW": "儲存一張成就畫面作為作品集紀錄。",
      "zh-CN": "保存一张成就画面作为作品集记录。"
    }
  },
  {
    image: "badges/IMG_9366.JPG",
    date: "2026-04-24",
    tag: "Growth",
    title: {
      en: "RedNote Snapshot 9366",
      "zh-TW": "小紅書成長快照 9366",
      "zh-CN": "小红书成长快照 9366"
    },
    description: {
      en: "Documented a growth-stage milestone update.",
      "zh-TW": "記錄成長階段中的里程碑更新。",
      "zh-CN": "记录成长阶段中的里程碑更新。"
    }
  },
  {
    image: "badges/IMG_9367.JPG",
    date: "2026-04-24",
    tag: "Achievement",
    title: {
      en: "RedNote Snapshot 9367",
      "zh-TW": "小紅書成就快照 9367",
      "zh-CN": "小红书成就快照 9367"
    },
    description: {
      en: "Recorded a key platform achievement screenshot.",
      "zh-TW": "保存平台關鍵成就畫面。",
      "zh-CN": "保存平台关键成就画面。"
    }
  },
  {
    image: "badges/IMG_9368.JPG",
    date: "2026-04-24",
    tag: "Badge",
    title: {
      en: "RedNote Snapshot 9368",
      "zh-TW": "小紅書勳章快照 9368",
      "zh-CN": "小红书勋章快照 9368"
    },
    description: {
      en: "Saved a notable badge-style progress result.",
      "zh-TW": "存檔一項代表性勳章成果。",
      "zh-CN": "存档一项代表性勋章成果。"
    }
  },
  {
    image: "badges/IMG_9369.JPG",
    date: "2026-04-24",
    tag: "Milestone",
    title: {
      en: "RedNote Snapshot 9369",
      "zh-TW": "小紅書里程碑快照 9369",
      "zh-CN": "小红书里程碑快照 9369"
    },
    description: {
      en: "Tracked another personal progress highlight.",
      "zh-TW": "追蹤另一個個人成長亮點。",
      "zh-CN": "追踪另一个个人成长亮点。"
    }
  },
  {
    image: "badges/IMG_9370.JPG",
    date: "2026-04-24",
    tag: "Growth",
    title: {
      en: "RedNote Snapshot 9370",
      "zh-TW": "小紅書成長快照 9370",
      "zh-CN": "小红书成长快照 9370"
    },
    description: {
      en: "Captured a steady growth benchmark screenshot.",
      "zh-TW": "截圖保存穩定成長指標。",
      "zh-CN": "截图保存稳定成长指标。"
    }
  },
  {
    image: "badges/IMG_9371.JPG",
    date: "2026-04-24",
    tag: "Achievement",
    title: {
      en: "RedNote Snapshot 9371",
      "zh-TW": "小紅書成就快照 9371",
      "zh-CN": "小红书成就快照 9371"
    },
    description: {
      en: "Archived a notable profile achievement.",
      "zh-TW": "保存個人頁面中的重要成就。",
      "zh-CN": "保存个人页面中的重要成就。"
    }
  },
  {
    image: "badges/IMG_9372.JPG",
    date: "2026-04-24",
    tag: "Badge",
    title: {
      en: "RedNote Snapshot 9372",
      "zh-TW": "小紅書勳章快照 9372",
      "zh-CN": "小红书勋章快照 9372"
    },
    description: {
      en: "Saved a badge capture for public portfolio history.",
      "zh-TW": "將勳章畫面納入公開作品集歷程。",
      "zh-CN": "将勋章画面纳入公开作品集历程。"
    }
  },
  {
    image: "badges/IMG_9373.JPG",
    date: "2026-04-24",
    tag: "Milestone",
    title: {
      en: "RedNote Snapshot 9373",
      "zh-TW": "小紅書里程碑快照 9373",
      "zh-CN": "小红书里程碑快照 9373"
    },
    description: {
      en: "Recorded a milestone indicator from the dashboard.",
      "zh-TW": "記錄儀表板中的里程碑指標。",
      "zh-CN": "记录仪表板中的里程碑指标。"
    }
  },
  {
    image: "badges/IMG_9374.JPG",
    date: "2026-04-24",
    tag: "Growth",
    title: {
      en: "RedNote Snapshot 9374",
      "zh-TW": "小紅書成長快照 9374",
      "zh-CN": "小红书成长快照 9374"
    },
    description: {
      en: "Documented another growth checkpoint image.",
      "zh-TW": "補充一張新的成長節點截圖。",
      "zh-CN": "补充一张新的成长节点截图。"
    }
  },
  {
    image: "badges/IMG_9375.JPG",
    date: "2026-04-24",
    tag: "Achievement",
    title: {
      en: "RedNote Snapshot 9375",
      "zh-TW": "小紅書成就快照 9375",
      "zh-CN": "小红书成就快照 9375"
    },
    description: {
      en: "Stored an achievement screen for timeline tracking.",
      "zh-TW": "存檔一張成就畫面以追蹤歷程。",
      "zh-CN": "存档一张成就画面以追踪历程。"
    }
  }
];

const uiText = {
  en: {
    htmlLang: "en",
    pageTitle: "RedNote Milestone Gallery",
    pageSubtitle:
      "A simple record of badges, achievements, and growth milestones.",
    heroKicker: "Curated Archive",
    themeLight: "Light",
    themeDark: "Dark",
    sortLabel: "Sort",
    sortNewest: "Newest",
    sortOldest: "Oldest",
    lastUpdatedPrefix: "Last updated",
    all: "All",
    empty: "No records found for this filter.",
    tags: {
      Badge: "Badge",
      Milestone: "Milestone",
      Growth: "Growth",
      Achievement: "Achievement"
    }
  },
  "zh-TW": {
    htmlLang: "zh-Hant",
    pageTitle: "小紅書里程碑作品集",
    pageSubtitle: "一個簡潔的勳章、成就與成長里程碑記錄。",
    heroKicker: "精選檔案",
    themeLight: "淺色",
    themeDark: "深色",
    sortLabel: "排序",
    sortNewest: "最新優先",
    sortOldest: "最早優先",
    lastUpdatedPrefix: "最後更新",
    all: "全部",
    empty: "此分類目前沒有紀錄。",
    tags: {
      Badge: "勳章",
      Milestone: "里程碑",
      Growth: "成長",
      Achievement: "成就"
    }
  },
  "zh-CN": {
    htmlLang: "zh-Hans",
    pageTitle: "小红书里程碑作品集",
    pageSubtitle: "一个简洁的勋章、成就与成长里程碑记录。",
    heroKicker: "精选档案",
    themeLight: "浅色",
    themeDark: "深色",
    sortLabel: "排序",
    sortNewest: "最新优先",
    sortOldest: "最早优先",
    lastUpdatedPrefix: "最后更新",
    all: "全部",
    empty: "该分类暂无记录。",
    tags: {
      Badge: "勋章",
      Milestone: "里程碑",
      Growth: "成长",
      Achievement: "成就"
    }
  }
};

const imageDimensions = {
  "badges/IMG_9364.JPG": { width: 750, height: 743 },
  "badges/IMG_9365.JPG": { width: 750, height: 742 },
  "badges/IMG_9366.JPG": { width: 750, height: 739 },
  "badges/IMG_9367.JPG": { width: 750, height: 744 },
  "badges/IMG_9368.JPG": { width: 750, height: 741 },
  "badges/IMG_9369.JPG": { width: 750, height: 735 },
  "badges/IMG_9370.JPG": { width: 750, height: 739 },
  "badges/IMG_9371.JPG": { width: 750, height: 744 },
  "badges/IMG_9372.JPG": { width: 1170, height: 1219 },
  "badges/IMG_9373.JPG": { width: 870, height: 902 },
  "badges/IMG_9374.JPG": { width: 1170, height: 1209 },
  "badges/IMG_9375.JPG": { width: 1170, height: 1222 }
};

const LANG_KEY = "rednote_lang";
const THEME_KEY = "rednote_theme";
const SORT_KEY = "rednote_sort";
const DEFAULT_LANG = "en";
const SUPPORTED_LANGS = ["en", "zh-TW", "zh-CN"];
const SUPPORTED_THEMES = ["light", "dark"];
const SUPPORTED_SORTS = ["newest", "oldest"];
const IMAGE_PATH_PATTERN = /^badges\/[a-zA-Z0-9._-]+\.(jpg|jpeg|png|webp|gif)$/i;

function detectBrowserLanguage() {
  const browserLangs = Array.isArray(navigator.languages)
    ? navigator.languages
    : [navigator.language];

  for (const lang of browserLangs) {
    const normalized = String(lang || "").toLowerCase();
    if (normalized.startsWith("zh")) {
      if (
        normalized.includes("tw") ||
        normalized.includes("hk") ||
        normalized.includes("mo") ||
        normalized.includes("hant")
      ) {
        return "zh-TW";
      }
      return "zh-CN";
    }
    if (normalized.startsWith("en")) {
      return "en";
    }
  }

  return DEFAULT_LANG;
}

const savedLanguage = localStorage.getItem(LANG_KEY);
let currentLanguage =
  savedLanguage && SUPPORTED_LANGS.includes(savedLanguage)
    ? savedLanguage
    : detectBrowserLanguage();
let activeTag = "All";
const savedTheme = localStorage.getItem(THEME_KEY);
let activeTheme =
  (savedTheme && SUPPORTED_THEMES.includes(savedTheme) ? savedTheme : null) ||
  (window.matchMedia("(prefers-color-scheme: dark)").matches ? "dark" : "light");
const savedSort = localStorage.getItem(SORT_KEY);
let activeSort = savedSort && SUPPORTED_SORTS.includes(savedSort) ? savedSort : "newest";

const elements = {
  pageTitle: null,
  pageSubtitle: null,
  heroKicker: null,
  languageToggle: null,
  themeToggle: null,
  gallery: null,
  filters: null,
  lightbox: null,
  lightboxImage: null,
  lightboxTag: null,
  lightboxTitle: null,
  lightboxDate: null,
  lightboxDescription: null,
  lightboxClose: null,
  sortLabel: null,
  sortSelect: null,
  lastUpdated: null
};

let isInitialized = false;

function cacheElements() {
  elements.pageTitle = document.getElementById("page-title");
  elements.pageSubtitle = document.getElementById("page-subtitle");
  elements.heroKicker = document.getElementById("hero-kicker");
  elements.languageToggle = document.getElementById("language-toggle");
  elements.themeToggle = document.getElementById("theme-toggle");
  elements.gallery = document.getElementById("gallery");
  elements.filters = document.getElementById("filter-controls");
  elements.lightbox = document.getElementById("lightbox");
  elements.lightboxImage = document.getElementById("lightbox-image");
  elements.lightboxTag = document.getElementById("lightbox-tag");
  elements.lightboxTitle = document.getElementById("lightbox-title");
  elements.lightboxDate = document.getElementById("lightbox-date");
  elements.lightboxDescription = document.getElementById("lightbox-description");
  elements.lightboxClose = document.getElementById("lightbox-close");
  elements.sortLabel = document.getElementById("sort-label");
  elements.sortSelect = document.getElementById("sort-select");
  elements.lastUpdated = document.getElementById("last-updated");
}

function hasRequiredElements() {
  return [
    elements.pageTitle,
    elements.pageSubtitle,
    elements.heroKicker,
    elements.languageToggle,
    elements.themeToggle,
    elements.gallery,
    elements.filters,
    elements.lightbox,
    elements.lightboxImage,
    elements.lightboxTag,
    elements.lightboxTitle,
    elements.lightboxDate,
    elements.lightboxDescription,
    elements.lightboxClose
  ].every(Boolean);
}

function t() {
  return uiText[currentLanguage] || uiText.en;
}

function getTagLabel(tag) {
  return t().tags[tag] || tag;
}

function getLocalizedValue(record, key) {
  const value = record[key];
  if (typeof value === "string") {
    return value;
  }
  return value[currentLanguage] || value.en;
}

function escapeHtml(value) {
  return String(value)
    .replaceAll("&", "&amp;")
    .replaceAll("<", "&lt;")
    .replaceAll(">", "&gt;")
    .replaceAll('"', "&quot;")
    .replaceAll("'", "&#39;");
}

function getSafeImagePath(path) {
  const candidate = String(path || "");
  return IMAGE_PATH_PATTERN.test(candidate) ? candidate : "";
}

function getWebpPath(path) {
  const safePath = getSafeImagePath(path);
  return safePath ? safePath.replace(/\.(jpg|jpeg|png|gif)$/i, ".webp") : "";
}

function getDateTimestamp(dateValue) {
  const parsed = Date.parse(dateValue);
  return Number.isNaN(parsed) ? 0 : parsed;
}

function getLatestBadgeDate() {
  const timestamps = badges.map((item) => getDateTimestamp(item.date)).filter(Boolean);
  if (!timestamps.length) {
    return "";
  }
  return new Date(Math.max(...timestamps)).toISOString().slice(0, 10);
}

function buildRenderableBadges() {
  const items = badges
    .map((item, id) => {
      if (activeTag !== "All" && item.tag !== activeTag) {
        return null;
      }
      const safeImagePath = getSafeImagePath(item.image);
      if (!safeImagePath) {
        return null;
      }
      return {
        id,
        safeImagePath,
        webpPath: getWebpPath(item.image),
        dims: imageDimensions[safeImagePath] || { width: 750, height: 750 },
        title: escapeHtml(getLocalizedValue(item, "title")),
        description: escapeHtml(getLocalizedValue(item, "description")),
        tagLabel: escapeHtml(getTagLabel(item.tag)),
        date: escapeHtml(item.date),
        timestamp: getDateTimestamp(item.date)
      };
    })
    .filter(Boolean);

  const sortFactor = activeSort === "oldest" ? 1 : -1;
  items.sort((left, right) => sortFactor * (left.timestamp - right.timestamp));
  return items;
}

function applyTheme() {
  document.documentElement.dataset.theme = activeTheme;
  if (elements.themeToggle) {
    elements.themeToggle.textContent =
      activeTheme === "dark" ? t().themeLight : t().themeDark;
  }
}

function renderLanguageSwitcher() {
  const options = [
    { code: "en", label: "EN" },
    { code: "zh-TW", label: "繁中" },
    { code: "zh-CN", label: "简中" }
  ];

  elements.languageToggle.innerHTML = options
    .map(
      (option) =>
        `<button type="button" class="lang-btn ${
          option.code === currentLanguage ? "active" : ""
        }" data-lang="${option.code}">${option.label}</button>`
    )
    .join("");

  elements.languageToggle.querySelectorAll(".lang-btn").forEach((button) => {
    button.addEventListener("click", () => {
      setLanguage(button.dataset.lang);
    });
  });
}

function renderPageText() {
  const locale = t();
  document.documentElement.lang = locale.htmlLang;
  document.title = locale.pageTitle;
  elements.pageTitle.textContent = locale.pageTitle;
  elements.pageSubtitle.textContent = locale.pageSubtitle;
  elements.heroKicker.textContent = locale.heroKicker;
  applyTheme();
}

function renderSortControl() {
  if (!elements.sortSelect || !elements.sortLabel) {
    return;
  }
  elements.sortLabel.textContent = t().sortLabel;
  elements.sortSelect.innerHTML = `
    <option value="newest">${escapeHtml(t().sortNewest)}</option>
    <option value="oldest">${escapeHtml(t().sortOldest)}</option>
  `;
  elements.sortSelect.value = activeSort;
}

function renderFooter() {
  if (!elements.lastUpdated) {
    return;
  }
  const latest = getLatestBadgeDate();
  elements.lastUpdated.textContent = latest
    ? `${t().lastUpdatedPrefix}: ${latest}`
    : "";
}

function renderFilters() {
  const tags = ["All", ...new Set(badges.map((item) => item.tag))];

  elements.filters.innerHTML = tags
    .map((tag) => {
      const label = tag === "All" ? t().all : getTagLabel(tag);
      return `<button type="button" class="filter-btn ${
        tag === activeTag ? "active" : ""
      }" data-tag="${escapeHtml(tag)}">${escapeHtml(label)}</button>`;
    })
    .join("");

  elements.filters.querySelectorAll(".filter-btn").forEach((button) => {
    button.addEventListener("click", () => {
      setActiveTag(button.dataset.tag);
    });
  });
}

function renderGallery() {
  const renderableBadges = buildRenderableBadges();

  if (!renderableBadges.length) {
    elements.gallery.innerHTML = `<p class="empty-state">${t().empty}</p>`;
    return;
  }

  elements.gallery.innerHTML = renderableBadges
    .map(
      (item, index) => {
        const eager = index === 0 ? "eager" : "lazy";
        const priority = index === 0 ? "high" : "auto";
        return `
      <article class="card">
        <picture class="card-thumb-wrap">
          <source type="image/webp" srcset="${item.webpPath}" />
          <img
            class="card-thumb"
            src="${item.safeImagePath}"
            alt="${item.title}"
            data-id="${item.id}"
            width="${item.dims.width}"
            height="${item.dims.height}"
            loading="${eager}"
            fetchpriority="${priority}"
            decoding="async"
          />
        </picture>
        <div class="card-body">
          <span class="tag">${item.tagLabel}</span>
          <h2>${item.title}</h2>
          <p class="date">${item.date}</p>
          <p class="card-desc">${item.description}</p>
        </div>
      </article>
    `;
      }
    )
    .join("");

  elements.gallery.querySelectorAll(".card-thumb").forEach((image) => {
    image.addEventListener("click", () => {
      const id = Number.parseInt(image.dataset.id || "", 10);
      if (!Number.isInteger(id) || !badges[id]) {
        return;
      }
      openLightbox(badges[id]);
    });
  });
}

function openLightbox(item) {
  const safeImagePath = getSafeImagePath(item.image);
  if (!safeImagePath) {
    return;
  }
  elements.lightboxImage.src = safeImagePath;
  elements.lightboxImage.alt = getLocalizedValue(item, "title");
  elements.lightboxTag.textContent = getTagLabel(item.tag);
  elements.lightboxTitle.textContent = getLocalizedValue(item, "title");
  elements.lightboxDate.textContent = item.date;
  elements.lightboxDescription.textContent = getLocalizedValue(item, "description");

  elements.lightbox.hidden = false;
  document.body.style.overflow = "hidden";
}

function closeLightbox() {
  elements.lightbox.hidden = true;
  elements.lightboxImage.src = "";
  document.body.style.overflow = "";
}

function rerenderAll() {
  renderPageText();
  renderSortControl();
  renderLanguageSwitcher();
  renderFilters();
  renderGallery();
  renderFooter();
}

function setLanguage(language) {
  if (!SUPPORTED_LANGS.includes(language)) {
    return;
  }
  currentLanguage = language;
  localStorage.setItem(LANG_KEY, currentLanguage);
  if (isInitialized) {
    rerenderAll();
  }
}

function setActiveTag(tag) {
  activeTag = tag;
  if (isInitialized) {
    renderFilters();
    renderGallery();
  }
}

function toggleTheme() {
  activeTheme = activeTheme === "dark" ? "light" : "dark";
  localStorage.setItem(THEME_KEY, activeTheme);
  applyTheme();
}

function getState() {
  return {
    currentLanguage,
    activeTag,
    activeTheme
  };
}

function initializeApp() {
  cacheElements();
  if (!hasRequiredElements()) {
    return false;
  }

  elements.lightbox.addEventListener("click", (event) => {
    if (event.target.dataset.close === "true") {
      closeLightbox();
    }
  });

  elements.lightboxClose.addEventListener("click", closeLightbox);

  document.addEventListener("keydown", (event) => {
    if (event.key === "Escape" && !elements.lightbox.hidden) {
      closeLightbox();
    }
  });

  elements.themeToggle.addEventListener("click", toggleTheme);
  if (elements.sortSelect) {
    elements.sortSelect.addEventListener("change", (event) => {
      const value = String(event.target.value || "");
      activeSort = SUPPORTED_SORTS.includes(value) ? value : "newest";
      localStorage.setItem(SORT_KEY, activeSort);
      renderGallery();
      renderSortControl();
    });
  }

  rerenderAll();
  isInitialized = true;
  if (document.body) {
    document.body.dataset.appReady = "true";
  }
  return true;
}

window.__rednoteGallery = {
  initializeApp,
  detectBrowserLanguage,
  setLanguage,
  setActiveTag,
  toggleTheme,
  renderGallery,
  renderFilters,
  openLightbox,
  closeLightbox,
  getState,
  getTagLabel,
  getLocalizedValue,
  badges
};

initializeApp();
